language: R
sudo: true
cache: packages

apt_packages:
  - libudunits2-dev

r_bitbucket_packages:
  - coolbutuseless/minilexer

r_github_packages:
  - thomasp85/ggraph
  - tidyverse/ggplot2
  - mailund/matchbox


r:
  - 3.2
  - 3.3
  - 3.4
  - oldrel
  - release
# - devel

after_success:
  - Rscript -e 'covr::codecov()'
  - Rscript -e 'covr::coveralls()'
